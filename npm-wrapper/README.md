# `@manhquy/dory`

Journal **lamp** (Session OS). Not the desk.

```bash
npx @manhquy/dory dory-serve --workspace /abs
```

Gõ `dory` vẫn là binary Rust. Package này **không** gắn bin `dory`.

Publish: GitHub Actions OIDC — same shape as `@manhquy/flow-skill`. See `RELEASE_CHECKLIST.md`.
