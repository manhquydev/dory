---
type: research
date: 2026-08-28
time: 16:12
wave: dory-cook-o
scope: copy N isolate law for unlock-2
---

# Research 02 — isolate copy-law for O

N paid: `bcf7c72` `scripts/dory-isolate-aoe5-flow-next.sh`. Do **not** `source`/`exec` N.

## Copy

Same as N: HERDR refuse, leftover mint table, land ELF sha `2ef20730…`, leftover ELF `3ba0e3bc…` stat-only, compound_stop 2357, attach 1910 `:331`, server 0242 `:340-353`, occupants `--no-session --no-skills --no-rules --no-extensions`, taxi `dory flow -- next`, poll PASS bytes (no `--wait`), copy journal + unlock sha **before** wipe, ISO prefix **new** `aoe5s.XXXXXX`.

## Delta

| N | O |
|---|---|
| Mint FAIL `00-idea` template; occupant PASS 00 | Factory mint **PASS `00-idea` + template `01-research`**; occupant PASS **01** |
| Taxi1 FAIL `00-idea`; no `01` | Taxi1 FAIL `01-research`; no `02` |
| Taxi2 `unlocked stage 1 (flow/01-research.md)` | Taxi2 `unlocked stage 2 (flow/02-scope.md)` |
| 01 sha == template `[FILL]` | 02 sha == template `[FILL]` |
| Self-refuse judge | Self-refuse judge **+ N** |
| Taxi pin 4 FLOW keys | Same + **`FLOW_HARNESS_DISABLE=1`** |
| Prefix `aoe5n.` | Prefix `aoe5s.` |
| Receipts `260827-ensure-aoe5-flow-next-*` | Receipts `260828-ensure-aoe5-flow-scope-*` |

## Factory mint 00 PASS is required

N forbade factory PASS. O **requires** factory PASS on **00** (N world-state already proved). Factory still **cấm** write PASS **01**.

If factory mints 00 template + 01 template, idx is still 1 (`research-01`). Taxi1 still FAIL 01. Taxi2 would unlock 02 with dirty 00 — dishonest. Assert 00 == PASS_00 before taxi1 and after taxi2.

## PASS 01 is mechanical

`scan_gate` only boxes + `[FILL`. Occupant fixture checks boxes and replaces FILL. This is **not** semantic research. Do not claim `gate-rules.md`. Do not fill 02. Do not walk 03–05.

## Live pins 16:12 factory

HEAD `d9ad7b5`. Rust `b544f5f`. PATH `dory` gone. Sock FileNotFound connectable=0. Leftover 5 mint MATCH. Land ELF `2ef20730…`. Leftover ELF `3ba0e3bc…`.
