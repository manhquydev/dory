---
type: accept
date: 2026-08-23
time: 09:05
status: pass
authority:
  - plans/reports/260822-skill-cli-socket-contract.md
  - plans/reports/260822-p5-s11-cook-brief.md
  - plans/reports/260823-0856-brainstorm-s11.md
  - rust/tests/p5_s11.rs
---

# §11 table — scored from `p5_s11` + green suite

`cargo test --offline --locked`: **99 passed** (new `p5_s11_named_driver_flow_on_spec_kit_clone` 1.37s).

`p5_real_repo` / `p5_inside` / `p5_live_loop` remain history. They are not this score.

| Clause | Score | Evidence |
|---|---|---|
| Driver is inside the pane | **PASS** | Test `agent start`s `s11driver` only (`/bin/bash` + `occ_s11`). No `pane_run` of the loop. Transcript has skill phrase + `$DORY_BIN` verbs. |
| `DORY_ENV=1` seen inside pane | **PASS** | Fixture `printenv`; test asserts `DORY_ENV=1`. |
| split / start / prompt / wait | **PASS** | Driver types skill split, starts `s11peer`, `prompt --wait`, `wait`. |
| Flow verdict, foreign judge | **PASS** | `FLOW_BIN` = flow-skill `flow.sh` (not `/bin/true`, not `eval/phase5-project`). Journal `{clone}/.dory/sessions/s1.jsonl` has `flow/invoke` + `flow/result`. Nonzero status allowed (clone has no `flow/`). |
| **Real** external repo | **PASS** | `git clone --no-hardlinks --depth 1` of `/home/manhquy/Downloads/spec-kit` (github.com/github/spec-kit). Not `flow-skill/flow/` copy. Not empty `/tmp`. |
| Not `eval/phase5-project` / header | **PASS** | Asserts reject that path; score is this table + the test body, not a header. |

Houses: `rg -i dory` on `flow-skill` empty. Original spec-kit has no `.dory`. No `rust/src` this cook. Classifier unchanged. No `omp` in cargo.

## Not this score

Factory `omp` driving Flow on spec-kit. `0847` phase files still paper.
