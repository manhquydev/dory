---
type: docs-init
lens: agents
date: 2026-08-29
time: 12:04
wave: dory-docs-1204
result: ACCEPT
head: 049e304
p_feat: f1c966c
rust_land: b544f5f
land_readme: 5ac82b10
leftover_readme: 68190a5f
kongming: UNAVAILABLE
---

# docs-init agents — AGENTS.md keep-or-cut

## Summary

Root `AGENTS.md` is process memory: costly-action denies only. Land README is `git show HEAD:README.md` (blob `5ac82b10` at `049e304`). Working `README.md` stays leftover mint `68190a5f`. Kongming skipped after two fails (fable usage limit; Codex 401). Filter already closed by operator: only costly-action denies.

## Findings

Before: 12 lines. After: 10 lines. Net −2.

| Line | Class | Verdict | Cost if missing |
|---|---|---|---|
| Title `# Agent deny list` | heading | keep | — |
| `Process memory only. Product WHY… WHERE…` | overview / docs duplicate | **cut** | none — `docs/README.md` already routes CHARTER / WHERE |
| leftover 5 + rust cite | deny | **keep** + **add** land README cite | fold leftover README via checkout; edit leftover rust |
| `git add -A` / `ak:git` | deny | keep | stage leftover 5 + ~398 untracked |
| sit `w13:t13` / `w13:p2R` + default `dory.sock` | deny | keep | occupy factory; start default sock. Git: `b544f5f` auto-start on sit |
| `herdr server stop` + close `wP`/`w15`/`w16`/`t13` | deny | keep | kill factory chairs |
| `source`/`exec` paid isolate scripts | deny | keep | recook N/O/P |
| cargo leftover dirty tree | deny | keep | rebuild leftover; isolate ELF miss treated as rebuild |
| isolate N/O/P ≠ company Phase 5 | deny | keep | Phase 5 lie |
| fill `flow/03-prd.md` / walk 04–05 | deny | keep | unpaid company cook |

## Land README

| Blob | Hash | Role |
|---|---|---|
| `git show HEAD:README.md` | `5ac82b102be4e4f0c621d779b9c4a3bb9819afbd` | land operator desk |
| working `README.md` | `68190a5ffa073c082aa318aad5ed032e13cc90e3` | leftover 5 mint |

Do not fold. Cite land. Working tree stays ` M`.

## Mining

- Window: 26 non-merge commits (under 90d / 300). Reverts 0. Fix-of-fix 0.
- CI skipped: no git remotes (`gh` authenticated, no Actions repo).
- Recurrence: operator re-type across `0918` / `0940` / `1012` / `1020` / `0054` / `1204` plus this prompt — ≥3 independent days.
- Path-scoped rules: none. No `CLAUDE.md`.
- Watchlist: empty.
- Already-enforced: none (no hooks/branch protection in this tree).

## Kongming

1. Task `kongming` → `ERROR_RATE_LIMITED_CHANGEABLE` on `claude-fable-5-high`.
2. Kit MCP `agent_kongming` → Codex 401 missing OpenAI bearer.

Proceeded on closed operator filter. No second filter invented.

## Recommendations

1. Keep the 10-line file. Do not put WHY/WHERE back.
2. Enforcement (recommend-only, not applied): deny `Bash(git add -A)` in the runtime permission file if the operator wants a hook. Markdown is not enforcement.
3. Do not restore working README from HEAD.

## Unresolved Questions

- None that block this wave. Kongming counsel remains unavailable until quota/auth recover.
