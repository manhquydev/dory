# REVIEW pane — USAGE / README / help test

You are `rv_usage`. Skills ON (ak:code-review). Read-only.

`git show HEAD:rust/src/main.rs` — no “Starts the server if needed”; still Bare-dory desk.
`git show HEAD:rust/src/attach.rs` module + help — no auto-start sentences.
`git show HEAD:README.md` sit = server then dory. `:31` still `22↔4↔0`.
HEAD `p5_attach` help test forbids auto-start sentence.

Do not rewrite leftover README Now. Do not invoke dory.

Write `/home/manhquy/Downloads/flow/dory/plans/reports/260827-ensure-server-no-spawn-review-usage.txt`
End: `USAGE_ACCEPT` or `USAGE_REJECT`.
