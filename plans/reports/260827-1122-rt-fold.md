---
type: review
date: 2026-08-27
time: 11:22
lens: Assumption Destroyer + leftover/fold
plan: 260827-1122-aoe-5-isolate-flow-judge
verdict: PLAN_REJECT
authority: plan files + live leftover mint + cited receipts
did_not: invoke dory; fold leftover; cargo leftover
---

# PLAN REVIEW — leftover/fold (Assumption Destroyer)

**PLAN_REJECT.** Review is the plan, not code quality. 8 load-bearing assumptions die inside this cook's life. Leftover 5 mint still MATCH this turn; the plan never pins them and then writes checks that destroy them.

Live leftover 5 (must stay; this pane `git hash-object` only):

| path | mint |
|---|---|
| `README.md` | `68190a5ffa073c082aa318aad5ed032e13cc90e3` |
| `rust/src/attach.rs` | `602479094e84d31ad6f017775a3d55aeb485c644` |
| `rust/src/main.rs` | `373d688636ff7315ccd665f450069d8284eb47ff` |
| `rust/src/server.rs` | `4de1554ad56e248cdcf42f02111b7389b08dae82` |
| `rust/tests/p5_attach.rs` | `9c28fc3e0f3666498a8952411242d5301f7911de` |

HEAD blobs ≠ mint. `git diff --stat HEAD -- rust/ README.md` = 5 files, +452/−108. Porcelain ` M` ×5. Leftover ELF sha `3ba0e3bc3e9630c8f0b544523610b2f4e784c6f47ad049ab0b67f10704f1bc14` (stat only). Isolate HEAD `b544f5f`. No rust hunk in this plan.

## Scope
- Files: `plan.md`, `phase-01-start.md`, `phase-02-independent-aoe-5-test.md`, `phase-03-review-and-ship.md`
- Focus: leftover 5 fold, rust cook, cargo leftover tree, ship pathspec, true/doctor/status as AOE 5, company Phase 5 claim, recook 1910/0242, isolate reset/NEWHEAD, p5 `flow-skill` bin drift
- Scout: leftover 5 are rust+README dirty vs `b544f5f`; `ak:git` is `git add -A`; scout copy-law includes 1910 `/bin/true`; research-02 (shipped) still taxis `doctor`

## Overall Assessment
The plan says the right bans in one column and then writes accept/ship/copy-law that violate those bans. Cook following phase-03 + scout + `ak:git` folds leftover 5 or recooks C/1910. Do not cook until the eight findings below are closed in the plan text.

---

## Finding 1: Leftover 5 mint is a word, not a table — snap-then-MATCH remints
- **Severity:** Critical
- **Location:** Phase 1 step 2; Phase 2 assert; plan Success Criteria
- **Flaw:** Plan folder never names the five paths or the five mint SHAs. Phase 1 snaps whatever worktree bytes exist, then later MATCH that snap. That is tautology: dirt after snap, or snap of HEAD blobs, both “pass.”
- **Failure scenario:** Cook copies 0242 `leftover_hash_snap` (six files, includes `desk.rs`) or C’s four-file KEEP set (drops `server.rs`). Or snaps `git rev-parse HEAD:rust/src/server.rs` (`dfca2ac5…`) instead of `hash-object` (`4de1554a…`). MATCH snap is green while mint is dead. Inverse: snap is honest, then C-style `git checkout HEAD --` attach/main/`p5_attach`/README to “clean rust” (Finding 2) remints four files; `server.rs` `4de1554a` stays and “leftover 5 MATCH snap” still lies if they re-snap.
- **Evidence:** `phase-01-start.md:51` “Snap leftover 5 `git hash-object`” — no paths, no SHAs. `phase-01-start.md:60` “Leftover hashes MATCH snap.” `phase-02-independent-aoe-5-test.md:42` “leftover 5 hash = mint” — mint undefined. `plan.md:106` same. Grep of `dory/plans/260827-1122-aoe-5-isolate-flow-judge/` for `68190a5|6024790|373d688|4de1554|9c28fc3|p5_attach` = **zero hits**. Contrast paid pin: `plans/reports/260827-ensure-server-no-spawn-ops.md:66-72`. 0242 snap is six files: `scripts/dory-isolate-flock-prompt.sh:162-166` (`README` attach main server `p5_attach` **+ `desk.rs`**). C checkout drops server: `plans/260827-1032-ensure-server-no-auto-spawn/phase-01-start.md:36`.
- **Suggested fix:** Hard-code the five-row mint table in plan + phase-01/02/03. Assert `git hash-object` == those SHAs, not == `$HASH_SNAP`. Ban 0242 six-file snap and C four-file KEEP as this wave’s leftover law.

## Finding 2: `rv_left` “no rust diff vs b544f5f” is a leftover-destruct accept — this plan has no rust hunk
- **Severity:** Critical
- **Location:** Phase 3 `rv_left`; Phase 1 rust/isolate cargo; plan trap 17
- **Flaw:** Leftover 5 **are** the rust+README worktree diff vs land `b544f5f`. Demanding “no rust diff” is demanding leftover gone. `git log -1 -- rust/` still `b544f5f` is compatible with mint. `git diff b544f5f -- rust/` is not. Review FAIL on the mint the plan claims to protect. Cook “fixes” by C leftover checkout + isolate `reset --hard NEWHEAD`. This plan has **no** rust hunk and **no** NEWHEAD.
- **Failure scenario:** `rv_left` sees `git diff --stat HEAD -- rust/` = attach/main/server/`p5_attach` dirty → Critical. Cook copies C: `git checkout HEAD -- rust/src/attach.rs rust/src/main.rs rust/tests/p5_attach.rs README.md` then isolate `reset --hard`. Mint 68190a5/6024790/373d688/9c28fc3 die. `server.rs` 4de1554a may survive (C left it). Or they `git restore rust/` and kill all five. Trap 17 forbids exactly this; `rv_left` requires it.
- **Evidence:** `phase-03-review-and-ship.md:30` “no rust diff vs `b544f5f`”. `phase-03-review-and-ship.md:19` “rust HEAD still `b544f5f`” (log vs diff conflated). `plan.md:74` “`NEWHEAD` / rust hunk — **không có**. Không checkout leftover. Không isolate reset.” `phase-01-start.md:46` “Do not modify leftover 5, `rust/**`” — leftover 5 ⊂ `rust/**` + README. C destroy path: `plans/260827-1032-ensure-server-no-auto-spawn/phase-01-start.md:36-47`. Live: leftover hashes MATCH mint; `git diff --stat HEAD -- rust/ README.md` is those five files only.
- **Suggested fix:** Delete “no rust diff.” Accept rust **commit** `b544f5f` via `git log -1 -- rust/` only. Accept leftover 5 ` M` + exact mint SHAs. Write “rust worktree dirty = leftover; do not checkout/restore/reset.”

## Finding 3: Ship pathspec is a decoy — `ak:git` is `git add -A`; leftover 5 are tracked ` M`
- **Severity:** Critical
- **Location:** Phase 3 ship + `rv_fold`; plan Herdr ship row
- **Flaw:** Ship tab is `ak:git`. That skill’s first tool is `git add -A`. Leftover 5 are modified tracked files. `-A` stages them. Pathspec allowlist never lists the five paths as deny. Risk text claims fold comes from `git add scripts/` “plus dirty rust” — `scripts/` cannot stage `README.md` / `rust/src/*`. The real fold is `-A`/`-u`/`rust/**`/`README.md`. Glob `plans/reports/260827-ensure-aoe5-flow-judge-*` is not a git pathspec unless the shell expands it; widening to `plans/reports/` or `git add -A` then “unstage leftover” is how leftover 5 land in a `feat`/`fix` group (`ak:git` groups rust as `code:`).
- **Failure scenario:** Ship follows `ak:git` literally. Cached includes leftover 5. Multi-commit splits rust leftover into `feat|fix` → leftover 5 become land. Mint dies; rust HEAD leaves `b544f5f`. Or they abort too late after commit. `rv_fold` “cached ⊆ allowlist” only works if they never run Tool 1.
- **Evidence:** `phase-03-review-and-ship.md:23` “Ship tab 1 pane, ak:git paper only.” `phase-03-review-and-ship.md:34-46` allowlist (script/plan/reports only; leftover 5 unnamed). `phase-03-review-and-ship.md:46` bans `-u`/`-A` in prose while 23 invokes the skill that runs `-A`. `phase-03-review-and-ship.md:80` “Ship folds leftover via `git add scripts/` plus dirty rust” — false mechanism. `plan.md:66` bans `-u`/`commit -a` but not `ak:git` Tool 1. `plan.md:89` “ship = ak:git”. `~/.cursor/skills/ak-git/SKILL.md:74-75` `git add -A`. `~/.cursor/skills/ak-git/references/workflow-commit.md:7` same; `:32-37` rust leftover → `code:` → `feat|fix`.
- **Suggested fix:** Ban `ak:git` for this paper, or override: `git add` **named files only** (no glob, no `-A`, no `-u`). Deny-list the five leftover paths + `rust/**` + leftover ELF. Pre-commit: `git diff --cached --name-only` must be the allowlist **and** `git hash-object` leftover 5 == mint **and** porcelain ` M` ×5. After commit: leftover still unstaged mint.

## Finding 4: Isolate cargo “if ELF missing” reopens leftover-tree cargo + isolate reset
- **Severity:** High
- **Location:** Phase 1 Risk; plan trap 9 vs trap 17
- **Flaw:** “Do not cargo leftover” then “Isolate cargo only if cook explicitly rebuilds `land-4b70f79` at `b544f5f`.” Rebuild at that commit is C’s `reset --hard NEWHEAD` + `cargo test --manifest-path …/land-4b70f79/rust/Cargo.toml`. This plan has no rust hunk. Isolate ELF already exists (mtime 10:56). Cargo in `/home/manhquy/Downloads/flow/dory` compiles leftover 5 dirty sources and remints leftover ELF `3ba0e3bc…`. Plan never pins that ELF sha. `rv_left` “no leftover cargo” has no measurement.
- **Failure scenario:** SIT_DORY missing or “stale.” Cook copies C risk table: isolate `reset --hard` then cargo. Or factory `cargo --manifest-path rust/Cargo.toml` “just to see.” Leftover ELF sha moves. Leftover 5 still ` M` but ELF is no longer the mint binary C held. Or isolate reset moves isolate HEAD off the no-hunk rule and invites “NEWHEAD=$(rev-parse HEAD)” after a mistaken leftover commit (Finding 3).
- **Evidence:** `phase-01-start.md:74` isolate cargo exception. `plan.md:66` “Cargo `/home/manhquy/Downloads/flow/dory`.” `plan.md:74` no isolate reset. C cargo+reset: `plans/260827-1032-ensure-server-no-auto-spawn/phase-01-start.md:47-49` and `:96`. Leftover ELF pin paid: `plans/reports/260827-ensure-server-no-spawn-ops.md:74`. `phase-03-review-and-ship.md:30` “no leftover cargo” — no sha. Live leftover ELF `3ba0e3bc3e9630c8f0b544523610b2f4e784c6f47ad049ab0b67f10704f1bc14`; isolate ELF exists; isolate HEAD `b544f5f`.
- **Suggested fix:** Close the isolate-cargo door. ELF missing = FAIL (already in phase-01:74 first sentence). Pin leftover ELF sha. `rv_left` = leftover ELF sha MATCH + no `Cargo.lock`/target mtime jump on leftover tree.

## Finding 5: Scout copy-law + shipped research-02 still call true/doctor/status AOE 5
- **Severity:** High
- **Location:** plan Scout/Nguồn; Phase 3 pathspec; Approaches C vs cited receipts
- **Flaw:** Plan Approach C and trap 1 correctly refuse `doctor`/`status`/`/bin/true` as AOE 5. Then the plan’s own scout tells cook to copy-law 1910 **`:308-309`** — that is `FLOW_BIN=/bin/true`. Nguồn research-02 (and Phase 3 **ships** it) still taxis `flow -- doctor` / `status` and writes `hello.sh`. Empty argv after `--` is `status` (rc=0 always; footer `Flow 0. status` = true chrome). Cook with plan + scout + research-02 open copies the paid body.
- **Failure scenario:** New script is 1910 with a renamed file. Taxi 1+2 both exit 0. Journal `bin=/bin/true` or `flow.sh`+`status`/`doctor`. Sit `Flow 0. status` / `Flow 0. doctor`. Receipts say AOE 5. 1012 already killed this. Occupant file is theater.
- **Evidence:** `plan.md:42` Approach C ban. `plan.md:58` trap 1. `plan.md:25` Nguồn includes `260827-1116-research-02-isolate-project.md`. `plans/reports/260827-1122-scout-aoe5.md:42` “Copy-law: 1910 `:25-35,:98-99,:177-224,:308-309`”. `scripts/dory-isolate-flow-sit.sh:308-309` `FLOW_BIN=/bin/true`. `plans/reports/260827-1116-research-02-isolate-project.md:85-86` “Taxi argv: `doctor` or `status`”. Same file `:102` `flow -- doctor`, `:151` judge = doctor, `:219` “Taxi `flow -- doctor`.” `phase-03-review-and-ship.md:39` ships that research. `rust/src/flow.rs:35-37` empty argv → `status`. `plans/reports/260827-1116-research-01-flow-judge.md:129-131` `status` rc=0 always; `doctor` = machine not project. `plans/reports/260827-1012-eval-aoe.md:29` 1910 true ≠ judge.
- **Suggested fix:** Strike 1910 `:308-309` from copy-law. Stamp research-02 **superseded** for taxi verb/PROJECT; do not ship it as live law, or banner the doctor/hello sketch dead. `rv_judge` fail if `bin` is `/bin/true` **or** argv0 is `status`/`doctor` **or** both codes are 0.

## Finding 6: p5 `flow-skill` bin drift — refuse is `-x`; test accepts any abs `flow.sh`
- **Severity:** High
- **Location:** plan trap 5; Phase 1 taxi/`FLOW_BIN` refuse; Phase 2 bin assert
- **Flaw:** Two `flow.sh` on disk, `cmp` DIFFERENT (205886 skill 22/8 vs 255864 `flow-skill` 20/8). p5 tests pin the old copy. Plan pins skill path in one sentence, then taxi helper inherits `$FLOW_BIN` and refuses only “not executable.” Phase 2 accepts “abs flow.sh (not `/bin/true`, not bare)” — the p5 path is abs and named `flow.sh`. p5 copy is `rw-r--r--` today: `-x` refuse trips, cook `chmod +x` (mutates `flow-skill`) or copies p5 const, or falls back to 1910 true (Finding 5).
- **Failure scenario:** Journal `bin=/home/manhquy/Downloads/flow/flow-skill/skills/flow/runner/flow.sh`. Gate bytes ≠ skill v0.25. p5 `status` tests look like a precedent. Cook calls it AOE 5 because `assert_ne!(FLOW_BIN, "/bin/true")` is what p5 already does.
- **Evidence:** `plan.md:62` pin skill; “Không pin p5 `flow-skill` copy.” `phase-01-start.md:32` `FLOW_BIN="$FLOW_BIN"`. `phase-01-start.md:50` refuse “`FLOW_BIN` not executable” — no exact-path pin, no refuse-if-p5. `phase-02-independent-aoe-5-test.md:39` “both `bin` == abs flow.sh (not `/bin/true`, not `flow.sh` bare)” — no skill path, no `flow-skill` ban. `rust/tests/p5_inside.rs:42` `FLOW_BIN = "…/flow-skill/skills/flow/runner/flow.sh"`. `p5_real_repo.rs:46` `p5_live_loop.rs:30` `p5_s11.rs:32` same. `plans/reports/260827-1116-research-01-flow-judge.md:110,151,225,260` drift + p5 pin cũ. Live `cmp` exit 1; skill `-x` 205886; `flow-skill` `-r` 255864.
- **Suggested fix:** Hard-assign `FLOW_BIN` to the skill abs path. Refuse if `$FLOW_BIN` contains `flow-skill` or ≠ that path. Phase 2: `bin` exact string + `cmp` vs skill file. Do not chmod the p5 copy.

## Finding 7: Ship subject + Outcome let cook claim company Phase 5 done
- **Severity:** High
- **Location:** plan Contract/Remainder; Phase 3 commit message
- **Flaw:** Remainder names unpaid default + 6-stage + semantic. Trap 12 bans “AOE 5 company xong.” Ship message is `feat(isolate): AOE 5 fail-then-pass flow judge`. Outcome/title read as AOE 5 paid. North star Phase 5 is a **completed** real project inside Dory, zero herdr/dsh on Dory runtime. This slice is isolate taxi + one idea file. 1012 still lists AOE 5 unpaid after 1910/0242. research-01: after this increment AOE 5 still unpaid. Paper `feat(isolate): AOE 5` is the claim.
- **Failure scenario:** Review/ops/HEAD treat Phase 5 closed. Next wave skips default/6-stage as “already AOE 5.” Founder sees leftover 5 folded or not; the calendar lie is the product bug.
- **Evidence:** `phase-03-review-and-ship.md:51` commit subject. `plan.md:1,14,20` title/outcome as AOE 5. `plan.md:33` “Full company Phase 5 … later” (paper shield). `plan.md:69` trap 12. `plan.md:110-115` Remainder. `plans/reports/260822-north-star-aoe.md:33` “A real project is completed inside Dory; Flow judges…”. `plans/reports/260827-1012-eval-aoe.md:29` AOE 5 unpaid. `plans/reports/260827-1116-research-01-flow-judge.md:26,246` company vs first increment; “Sau slice này AOE 5 vẫn **unpaid**.”
- **Suggested fix:** Commit `docs(isolate): first-slice flow.sh gate 00-idea fail-then-pass` (not `feat` AOE 5). Outcome line: “not company Phase 5.” `rv_judge` Critical if any receipt says Phase 5 / AOE 5 done.

## Finding 8: Recook 1910/0242/C is the default path if Findings 2–5 stand
- **Severity:** High
- **Location:** plan Constraints/Scout; Phase 1 copy-law + 0242 server start
- **Flaw:** Non-goal is “Recook 1910/0043/0227/0242/C.” Implementation is copy-law those scripts, start server “0242 HOME/PATH/PI_CODING,” and (scout) copy 1910 `:308-309`. 0242 leftover snap is the six-file function. C is the leftover checkout + NEWHEAD + isolate reset + leftover-tree-adjacent cargo. There is no new leftover law — only “don’t exec.” Cook execs the law by pasting it.
- **Failure scenario:** `scripts/dory-isolate-aoe5-flow-judge.sh` is 0242 + 1910 true taxi + C leftover ritual. Paid scripts unchanged (non-goal satisfied on file path). Product is a recook. Leftover 5 fold or remint. Isolate reset happens anyway.
- **Evidence:** `plan.md:21-22` no exec/recook. `phase-01-start.md:45-46` copy-law 1910+0242, do not exec. `phase-01-start.md:53` “Start isolate server (0242 HOME/PATH/PI_CODING).” `plans/reports/260827-1122-scout-aoe5.md:42` copy-law includes 1910 `:308-309`. `scripts/dory-isolate-flock-prompt.sh:162-166,309,546` snap/match leftover (plus `desk.rs`). `plans/260827-1032-ensure-server-no-auto-spawn/phase-01-start.md:36-47` C leftover checkout + `NEWHEAD` + isolate reset. `plans/reports/260827-1116-research-02-isolate-project.md:169` “Recook paid scripts — Ban” then `:59-76` copy those compounds.
- **Suggested fix:** Copy-law whitelist by **behavior** (stop compound, sit pane ≠ t13, factory sock, leftover **five-SHA** assert), never by 1910 taxi lines or C KEEP. Phase 1 refuse if script `grep`s `/bin/true`, `NEWHEAD`, `git checkout HEAD --`, `reset --hard`, `cargo` on leftover tree.

---

## Edge Cases (scout)
- Leftover 5 ⊂ rust worktree; any “clean rust” accept is a fold.
- `ak:git` groups leftover rust as `code:` and commits it.
- 0242 snap + `desk.rs` makes a sixth “leftover” and hides `server.rs` remint if they drop it.
- p5 `flow-skill` `-x` miss → chmod or `/bin/true`.
- Empty `dory flow --` → `status` ≡ true footer.
- Isolate cargo “at b544f5f” ⇒ isolate reset even when trap 17 forbids it.
- Shipping research-02 republishes doctor/hello as HEAD law next to this plan.

## Recommended Actions
1. Pin leftover 5 path+SHA table; MATCH mint, not snap.
2. Delete `rv_left` rust worktree-diff; keep `git log -1 -- rust/` = `b544f5f` + leftover ` M` ×5.
3. Replace `ak:git` with named-file `git add`; deny leftover 5 / `rust/**`; drop the `git add scripts/` fold myth.
4. Close isolate cargo; pin leftover ELF `3ba0e3bc…`.
5. Strike 1910 `:308-309` copy-law; supersede research-02 taxi; `rv_judge` fail true/status/doctor.
6. Pin skill `FLOW_BIN` exact; refuse `flow-skill`.
7. Commit/outcome: first slice, not company Phase 5.
8. Recook refuse: no `/bin/true`, `NEWHEAD`, leftover checkout, isolate reset, leftover cargo in the new script.

## Metrics
- Type Coverage: n/a (plan review)
- Test Coverage: n/a
- Linting Issues: n/a
- Findings: 8 (3 Critical, 5 High)

## Unresolved Questions
- Whether ship should omit `260827-1116-research-02-isolate-project.md` entirely vs banner it superseded.
- Whether leftover ELF sha stay-check belongs on every phase or only `rv_left`.

PLAN_REJECT. Do not cook. Do not fold leftover. Do not cargo leftover.
