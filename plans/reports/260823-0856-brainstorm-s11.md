---
type: brainstorm
date: 2026-08-23
time: 08:56
status: locked
feeds: plans/260823-s11-real-repo
unholds: plans/reports/260822-p5-s11-cook-brief.md
authority:
  - CHARTER.md
  - plans/reports/260822-skill-cli-socket-contract.md
  - plans/reports/260822-p5-s11-cook-brief.md
  - plans/reports/260822-1942-brainstorm-occupant-lock.md
  - plans/reports/260823-layer4f2-omp-factory.md
---

# Brainstorm — contract §11 (unhold 1a)

## Prerequisite (evidence, not intent)

Occupancy factory 4f2: `omp` start `unknown`, prompt Dory CLI, report landed, `get` = `done`. Plan `260823-0011` completed. 1a was HOLD so occupancy was not skipped. That reason is gone.

## Slice contract

| Field | Closed |
|---|---|
| Outcome | Named occupant **inside** a pane (`DORY_ENV=1`) splits, starts, prompts, waits, and taxis `dory flow -- status` on a **real** external repo. A stranger can mark every §11 clause PASS from the new test’s files. |
| Constraints | Brief `260822-p5-s11-cook-brief.md`. Three houses. Classifier frozen. No `--kind`. No Xia `--copy`. No `omp` in `cargo test`. No rust/src. Original `/home/manhquy/Downloads/spec-kit` not mutated. `flow-skill` zero `dory` bytes. |
| Non-goals | Factory omp §11; fill §5 verbs; Flow gates in Dory; flip `0847` paper; allowlist `omp`; copy `flow-skill/flow/` into the clone |
| Acceptance | `cargo test --offline --locked` green **and** `p5_s11` world-state fills the scout table (driver / env / verbs / foreign verdict / real repo / not-header) as PASS. `p5_real_repo` stays history, not §11. |

HOLD SCOPE. Occupancy is not reopened.

## Approaches

| | Cách | Giả định nặng | Gãy trước |
|---|---|---|---|
| **A** | Chỉ factory omp + spec-kit | 4f2 spinner không nuốt Flow | `--offline` CI không chứng; wait 180s đã trượt |
| **B** (chọn) | Cook brief 1a: `/bin/bash` fixture đọc `SKILL.md`, clone local spec-kit, không `pane_run` | spec-kit vẫn ở path khóa; server 1 luồng + poll read | Path spec-kit thiếu trên máy khác |
| **C** | A+B cùng slice | Cả hai xong một cook | Phạm vi phình; 4f2 wait budget chưa khóa |

**B** rẻ bỏ: xóa một test + fixture. Không đụng classifier.

Unhold 1a **as §11**, not as a substitute for occupancy.

## Locked

1. Judged original: `/home/manhquy/Downloads/spec-kit` (git, no `flow/`, no `.dory`). Clone `--no-hardlinks` into temp. Nonzero `status` allowed.
2. Test may `agent start` the named driver only; then `pane read` / `agent get`. No `pane run`. No blocking `wait-output` while the driver still calls `$DORY_BIN`.
3. Driver argv: `/bin/bash <occ_s11> <clone> <SKILL.md> <FLOW_BIN>`. Print `DORY_OCC_READY` so allowlisted bash start settles. Peer = existing READY `sh` loop.
4. No `rust/src`. No `omp` in cargo. No 0847 flip.

## Unresolved

- Machines without that spec-kit path fail the new test (accepted; this tree has it).
- Factory omp on a real repo is a later plan, not this cook.
