---
type: research
date: 2026-08-27
time: 16:56
wave: isolate-next
cwd: /home/manhquy/Downloads/flow/dory
scope: copy-law for scripts/dory-isolate-aoe5-flow-next.sh
did_not: implement; exec leftover/isolate ELF; cargo leftover; start default sock; sit t13; source/exec paid isolate
---

# Copy-law — isolate `dory flow -- next`

**N cook = new file.** Duplicate AOE5 law. Do not `source`/`exec` paid scripts. Taxi verb changes. Journal + sit needles **must** change with it. Not company Phase 5.

Paid body read: `scripts/dory-isolate-aoe5-flow-judge.sh` (WT line-matches 16:38 `git show HEAD` cites). Cite rust via `git show HEAD`. Leftover 5 dirty on purpose.

## Ranked choice

1. **Do this:** copy AOE5 sit/stop/server/occupant-PASS/ISO-bin/mint. New script. Taxi argv `flow -- next`. Rewrite journal + glance needles. Self-refuse now lists `*dory-isolate-aoe5-flow-judge.sh`. World-state `$ISO_REAL/flow/01-research.md` exists after taxi2.
2. **Reject:** `source` AOE5 / 1910 / 0242. Taxi-only patch that keeps `GATE`/`clean` / `Flow *. gate`. Recook 1910 sit.bin. `--wait`. Rust. Company-Phase-5 subject.
3. **Never:** leftover ELF / isolate ELF on factory XDG / leftover cargo / default sock / sit t13.

## 1. COPY vs NEVER source/exec

Copy = paste text into `scripts/dory-isolate-aoe5-flow-next.sh`. Never `source` / `.` / `exec` the donors.

### COPY (AOE5 = paid skeleton)

| Block | AOE5 start:end | Also law |
|---|---|---|
| umask, pins, `HERDR_ENV`, factory `DORY_*`/`FLOW_*` refuse | 6:55 | keep FLOW refuse |
| `sock_connectable` | 61:73 | sit 42:54 |
| `iso` / `iso_mut` / `iso_identity_ok` | 75:100 | sit identity 56:67 |
| `compound_stop` | 102:134 | **sit 69:100 verbatim** |
| `wipe_iso` `factory_must_dead` `repo_dory_stat` | 136:167 | sit 103:134 |
| `leftover_mint_ok` `desk_head_ok` `path_dory_empty` | 169:221 | trap 23 |
| `pane_fields` `copy_journal` (new receipt names) | 239:259 | — |
| `idea_still_fail` `write_pass_want` `coord_prompt_write_pass` | 269:278, 337:383 | no `--wait` |
| `poll_pass_file` | 385:396 | trap 21 — file bytes, not idle |
| `teardown` `fail` EXIT trap | 398:419 | — |
| `SIT_DORY` realpath + leftover-target + leftover-iso + factory-XDG refuse | 423:446 | AOE5 extras; 1910 only 182:186 |
| `FLOW_BIN` abs `flow.sh`, not `/bin/true` | 456:468 | trap 5, 24 |
| sit pane/tab ≠ cook / p2R / wP / t13 | 474:509 | sit 189:224 |
| split dirty sit if needles already visible | 518:541 | sit 229:254 pattern |
| mint snap + ISO identity + FAIL idea + PASS_WANT ≠ factory | 543:580 | trap 2, 4 |
| **ISO/bin pin** | **582:586** | 0242 340:344; **1910 missing** |
| **setsid server** | **588:595** | **0242 346:353** |
| isolate sock poll + `factory_must_dead` | 597:615 | sit 284:302 |
| workspace list **no attach**; occupants; ready `╭──`; `report idle` | 617:672 | — |
| taxi1 rc=1 then occupant then taxi2 rc=0 | 674:743 | structure only |
| **attach** | **692:698** | **sit 331:337 verbatim** |
| leftover/desk/repo/path after + journal copy **before** wipe | 751:771 | trap 14, 22 |

### CHANGE (not copy-as-is)

| Block | AOE5 start:end | New |
|---|---|---|
| header / `$0` identity | 1:4, 38:43 | this file is `…-flow-next.sh`; refuse judge+1910+0043+0227+0242+hop |
| `JOURNAL_COPY_*` | 19:20 | `dory-aoe5-flow-next-journal.jsonl` |
| `self_refuse_paid` | 223:237 | regex **adds** `dory-isolate-aoe5-flow-judge` |
| `taxi` | 261:266 | `"$SIT_DORY" flow -- next` — same env prefix |
| `journal_taxi1` | 280:306 | see §2 |
| `journal_taxi2` | 308:335 | see §2 |
| `visible_has_needles` | 511:516 | `Flow 1. next` / `Flow 0. next` |
| `ATTACH_CMD` needle case | 693:695 | those next needles, not gate |
| wait-output | 700:704, 745:749 | `Flow 1. next` then `Flow 0. next` |
| echo labels | 786:790 | `NEEDLE*=Flow *. next`; `aoe5-next=PASS` |
| mktemp | 553 | distinct prefix (`aoe5n.` / `next.`) |

### NEVER source / exec / recook

- `scripts/dory-isolate-aoe5-flow-judge.sh` — **now paid**
- `scripts/dory-isolate-flow-sit.sh` (1910)
- `scripts/dory-isolate-flock-roster.sh` (0043)
- `scripts/dory-isolate-flock-report.sh` (0227)
- `scripts/dory-isolate-flock-prompt.sh` (0242)
- `scripts/dory-flock-hop.sh`
- 1910 taxi `FLOW_BIN=/bin/true` sit `:308-309`
- leftover ELF `…/dory/rust/target/debug/dory`
- isolate ELF on factory XDG
- `cargo` leftover tree
- start `/run/user/1000/dory/default`
- sit `w13:t13` / `p2R` / `wP`

## 2. Taxi() only? No.

`taxi()` argv is the **verb** delta. Journal + glance **must** follow or the script false-fails / false-passes.

AOE5 paid taxi:

```
"$SIT_DORY" flow -- gate 00-idea
```

`scripts/dory-isolate-aoe5-flow-judge.sh:261-266`

New:

```
"$SIT_DORY" flow -- next
```

Same line env: `HOME=$ISO_REAL/home` `DORY_SOCKET=$ISO_SOCK` `DORY_ENV=1` `DORY_WORKSPACE_DIR=$ISO_REAL` `FLOW_PROJECT_ROOT=$ISO_REAL` `FLOW_BIN=$FLOW_BIN` `FLOW_LOG_DISABLE=1` `DO_NOT_TRACK=1`. Refuse factory `FLOW_*` at entry (`:52-54`).

| Check | AOE5 (gate) | Next | Why |
|---|---|---|---|
| taxi1 stdout | `"GATE stage 00-idea"` `:303-304` | **`FAIL` + `00-idea`** | `next` dirty → `FAIL: gate for stage 00-idea is not clean.` `flow.sh:966`. No `GATE stage` line (`gate` verb is `:1426`). |
| taxi2 stdout | `"clean"` `:332-333` | **`unlocked stage 1`** | `next` unlock → `PASS: stage $cur gate clean -> unlocked stage $nidx (flow/$nxt.md)` `flow.sh:1026`. Word `clean` is in **both** next FAIL (`not clean`) and PASS (`gate clean`). Copying `:332` cannot tell taxi1 from taxi2. |
| journal `args` | not checked; paid rows are `["gate","00-idea"]` | require **`["next"]`** | AOE5 journal receipt. Prevents recooked gate taxi claiming N. |
| taxi2 world | none | **`$ISO_REAL/flow/01-research.md` exists** | `flow.sh:1024-1026`. Do not fill it. |
| codes / bin / count | `[1,0]`, abs `flow.sh`, two `flow/result` | **keep** | trap 22 |

AOE5 taxi2 `clean` was the gate-verb line `GATE stage 00-idea:\n  clean\n` (`260827-ensure-aoe5-flow-judge-journal.jsonl:4`). That string **does not exist** on `next`.

## 3. Sit needles

Glance = `Flow {n}. {arg0}` (`git show HEAD:rust/src/desk.rs:3450-3465`). `arg0` = first token after `flow --`.

| | AOE5 | Next |
|---|---|---|
| taxi1 sit | `Flow 1. gate` | **`Flow 1. next`** |
| taxi2 sit | `Flow 0. gate` | **`Flow 0. next`** |

Patch every AOE5 site: `:511-516` `:693-695` `:700` `:745` `:786-787`. Keep sit attach argv (`:331`) — only the **match strings** change.

## 4. Self-refuse — judge is paid

AOE5 `:223-237` regex: sit / prompt / roster / report / hop. **Omits itself.**

New regex **must** add `dory-isolate-aoe5-flow-judge`. `$0` case (`:38-43`) must refuse those **plus** `*dory-isolate-aoe5-flow-judge.sh`. Identity echo: this file is `dory-isolate-aoe5-flow-next.sh`.

Keep `/bin/true` chrome refuse (`:234-235`).

## 5. `SIT_DORY` pin = land-4b70f79 ELF

Scripts **do not** hardcode `land-4b70f79` (`260827-1638-eval-iso.md:45`). Cook passes:

`/home/manhquy/.cache/dory-isolates/land-4b70f79/rust/target/debug/dory`

sha `2ef20730…` ino `1065212`. Land = `b544f5f` no-spawn (`git show HEAD:rust/src/attach.rs:326-332`).

Copy AOE5 refuse (`:433-445`):

- `$REPO_TARGET/*` → leftover `rust/target` (1910 only has this: sit `:182-186`)
- `$LEFTOVER_ISO/*` → `flock.6yaatuxg`
- `$FACTORY_XDG/*` → factory XDG ELF
- `~/.local/bin/dory`

Empty `SIT_DORY` refuse. All exec = `"$SIT_DORY"`, never PATH `dory`.

## 6. ISO/bin — copy AOE5, not 1910

| | ISO/bin + setsid `PATH=$ISO_REAL/bin:$PATH` |
|---|---|
| AOE5 | **has** `:582-591` |
| 0242 | has `:340-351` |
| **1910 sit** | **no** — setsid `:277-281` inherits factory PATH |

After PATH name gone, 1910 children = CNF, not leftover spawn. Recook 1910 **do-not**. Occupant `dory` in coord prompt **needs** the pin. Copy AOE5.

## 7. Leftover 5 mint (exact)

Copy `leftover_mint_ok` `:173-178` / trap 23 / ops `:72-76`:

| path | sha1 |
|---|---|
| `README.md` | `68190a5ffa073c082aa318aad5ed032e13cc90e3` |
| `rust/src/attach.rs` | `602479094e84d31ad6f017775a3d55aeb485c644` |
| `rust/src/main.rs` | `373d688636ff7315ccd665f450069d8284eb47ff` |
| `rust/src/server.rs` | `4de1554ad56e248cdcf42f02111b7389b08dae82` |
| `rust/tests/p5_attach.rs` | `9c28fc3e0f3666498a8952411242d5301f7911de` |

`desk.rs` worktree == `HEAD:rust/src/desk.rs` = `4c788562e4fdda10c8edd2878ed1fdd46050c218`. Porcelain ` M` ×5 unstaged. Not land.

## 8. Ship pathspec only

AOE5 paid ship: named files, `git add --`, no `-A`/`-u`, no `ak:git` if it `-A`. Subject `feat(isolate): fail-then-pass flow.sh gate`. Rust stayed `b544f5f`. `phase-03:34-54` `ops.md:16-22` trap 25–26.

N analog allowlist (named files, no glob-as-git):

```
scripts/dory-isolate-aoe5-flow-next.sh
plans/<this-plan>/
plans/reports/260827-1656-research-02-isolate-next.md
plans/reports/260827-ensure-aoe5-flow-next-*
```

**Never cached:** leftover 5, `rust/**`, `README.md`, paid `dory-isolate-aoe5-flow-judge.sh` recook, 1910/0043/0227/0242/hop.

`git log -1 -- rust/` stays `b544f5f` `fix(attach): do not auto-start server on sit`.

Subject **must not** claim company Phase 5. Isolate unlock slice only. Example: `feat(isolate): fail-then-pass flow.sh next`. Not `AOE 5 done`. Not Phase 5.

## 9. Trap 13 is cook-scoped, not forever

1122 trap 13 (`plan.md:70`): `assess` / `next` / `card` / PASS-path `check` (mutate harness) — **không gọi**. That cook’s án was `gate 00-idea`. Remainder (`:124-128`) names **full 6-stage + semantic** unpaid — not “never unlock.”

16:38: trap 13 forbade `next` **inside that cook**. Unlock = named remainder’s first world-state. One `flow -- next`. No `card`. No fill `01`. No 02–05. `260827-1638-eval-next.md:49` `260827-1638-eval-synth.md:111`.

Founder 2105 forbids Dory **button** `dory flow next`, not taxi `dory flow -- next`. Dory has no `next` logic (`git show HEAD:rust/src/flow.rs:3`).

## 10. Factory-chair bleed

| Law | Copy | Ban |
|---|---|---|
| Attach | sit `:331` = `cd "$ISO_REAL" && DORY_SKIP_ONBOARD=1 XDG_RUNTIME_DIR=$ISO_REAL env -u DORY_SOCKET -u DORY_ENV -u DORY_RECYCLE "$SIT_DORY" attach` | missing isolate XDG → C no-spawn ping factory, or leftover ELF mints default. `herdr pane run`. |
| Stop | sit `compound_stop` `:69-100` — identity + sock real + not symlink + under `ISO_REAL` then `XDG_RUNTIME_DIR="$ISO_REAL" env -u DORY_SOCKET "$SIT_DORY" server stop` | `iso()` / `DORY_SOCKET=` on stop |
| Server | 0242 `:346-353` `setsid` + isolate `XDG`/`HOME`/`PATH=ISO/bin` + `PI_CODING_AGENT_DIR` **only that line** | factory PATH; export HOME script-scope |
| Prompt | AOE5 `:358-383` `prompt --timeout` **no `--wait`** | `--wait` / `occ.report=Working` (0242 trap 10) |
| PASS | `poll_pass_file` `:385-396` cmp bytes ~180s | taxi2 on coord idle; factory-write PASS |

Factory sock: connectable=0 throughout. Existence alone ≠ FAIL (`:490-491`).

## Trade-offs

| | Copy AOE5+delta | Source AOE5 | Taxi-only keep GATE/clean |
|---|---|---|---|
| Isolation | new file, paid refuse | bleed paid body | false án |
| Complexity | one script | hidden coupling | looks small, wrong |
| Maintenance | duplicate ~790 lines | one edit breaks two | journal lies |
| Adoption | same as AOE5 | forbidden trap 3 | cook fails or launders gate |

## Án (cook)

1. Taxi1 rc=1; journal `bin`=abs `flow.sh` `args=["next"]` stdout `FAIL`+`00-idea`; sit `Flow 1. next`.
2. Occupant writes PASS `00-idea.md`. Poll bytes. No `--wait`.
3. Taxi2 rc=0; same `bin`; stdout `unlocked stage 1`; **`$ISO_REAL/flow/01-research.md` exists**; sit `Flow 0. next`.
4. Two `flow/result` codes `[1,0]`. Journal copied before wipe. Factory sock connectable=0. Leftover 5 mint. PATH `dory` empty. Repo `.dory/` unchanged. Rust land `b544f5f`.

## Copy-table

| Copy | From | Into |
|---|---|---|
| stop | sit `:69-100` / AOE5 `:102-134` | new `compound_stop` |
| attach | sit `:331-337` / AOE5 `:692-698` | new `ATTACH_CMD` |
| setsid + ISO/bin | 0242 `:340-353` / AOE5 `:582-595` | new server start |
| mint / desk / PATH empty | AOE5 `:169-221` | same table |
| occupant PASS + poll file | AOE5 `:337-396` | same; no `--wait` |
| SIT_DORY refuse extras | AOE5 `:433-445` | not 1910-only |
| taxi env prefix | AOE5 `:262-266` | argv `next` only |

## Do-not

- `source`/`.`/`exec` judge / 1910 / 0043 / 0227 / 0242 / hop
- Recook paid bodies. Exec leftover ELF `3ba0e3bc…` or isolate ELF `2ef20730…` on factory XDG
- `cargo` leftover cwd. Fold leftover 5. `git add -A`/`-u` / `rust/**`
- Start default sock. Sit `t13`/`p2R`/`wP`. `herdr server stop`
- Keep journal `GATE`/`clean` or sit `Flow *. gate`
- `prompt --wait`. Factory-write PASS. Taxi2 on idle
- `iso()` / `DORY_SOCKET=` on stop. 1910 `/bin/true` taxi
- Hardcode `land-4b70f79` in script (env pin). Recook 1910 sit.bin
- Dory button `next`/`card`/`check`. `dory flow next` without `--`
- Fill `01-research.md`. Walk 02–05. Semantic `gate-rules.md`
- Claim N / AOE5 = company Phase 5. Subject that pretends it
- Implement this research

## Evidence

- AOE5 taxi/journal/needles/refuse/mint/bin/stop/attach/poll: `scripts/dory-isolate-aoe5-flow-judge.sh:38-43` `:52-54` `:102-134` `:173-178` `:223-237` `:261-266` `:280-335` `:385-396` `:433-445` `:511-516` `:582-595` `:692-749`
- sit attach + stop: `scripts/dory-isolate-flow-sit.sh:69-100` `:182-186` `:277-281` `:308-309` `:331-337`
- 0242 setsid: `scripts/dory-isolate-flock-prompt.sh:340-353`
- `next` stdout: `~/.claude/skills/flow/runner/flow.sh:966` `:1024-1026` `:1426`
- journal shape (gate, not next): `plans/reports/260827-ensure-aoe5-flow-judge-journal.jsonl:1-4`
- trap 13 cook-scope + remainder: `plans/260827-1122-aoe-5-isolate-flow-judge/plan.md:70` `:124-128` `:80-89`
- ship pathspec + subject: `plans/260827-1122-aoe-5-isolate-flow-judge/phase-03-review-and-ship.md:34-54` `plans/reports/260827-ensure-aoe5-flow-judge-ops.md:16-22` `:56-66`
- ISO/bin vs 1910; SIT_DORY env; leftover 5: `plans/reports/260827-1638-eval-iso.md:24-25` `:37-39` `:45-47` `:73`
- N cook spec: `plans/reports/260827-1638-eval-next.md:47-76` `plans/reports/260827-1638-eval-synth.md:103-125`
- doors: `plans/reports/260827-1632-eval-doors.md:17-22` `:27`
- glance / no Dory `next`: `git show HEAD:rust/src/desk.rs:3450-3465` `git show HEAD:rust/src/flow.rs:3`
- C no-spawn: `git show HEAD:rust/src/attach.rs:326-332`

## Unresolved

- Exact new plan dir name (ship allowlist placeholder `plans/<this-plan>/`).
- Commit subject final wording — only constraint: isolate slice, not company Phase 5.

**paid** — AOE5 gate slice; C; leftover 5 mint; copy-law above.
**unpaid** — this script until cooked; company Phase 5; 6-stage; `--wait`; default sit.
**next-fix (one)** — implement `scripts/dory-isolate-aoe5-flow-next.sh` per copy-table.
**do-not** — source paid; leftover/factory ELF; cargo leftover; default sock; sit t13.
