---
type: brainstorm-accept
date: 2026-08-29
time: 12:04
advice: factory GO
head: 049e304
p_feat: f1c966c
rust_land: b544f5f
---

# Brainstorm accept — docs route after P

## Contract

| Field | Closed |
|---|---|
| **Outcome** | Evergreen WHERE exists under `docs/`. CHARTER points there. Journal records P ship. Agent context denies leftover fold / default sit / `git add -A`. P plan frontmatter matches 100% phases. Untracked P receipts that already exist may join the named reports set. |
| **Constraints** | No product rust. No leftover 5 edit (working README stays mint `68190a5f`). Cite land README as `git show HEAD:README.md`. No default sock. No sit `t13`. No `herdr server stop`. Keep `wP`/`w15`/`w16`/`t13`. No recook P. No fill 03. Docs own WHY/WHERE only. |
| **Non-goals** | Company Phase 5. Default occupancy. Leftover fold. Walk 04–05. `card`. Semantic. Push. Rewrite leftover README. ADR farm. |
| **Acceptance** | `docs/README.md` routes CHARTER + paid isolate owners + unpaid remainder. CHARTER has one WHERE link. `ak journal` entry for P. `AGENTS.md` deny-list only. Red-team critical 0 on stale Phase-5 / leftover-fold / t13-sit claims. Leftover 5 still `M`. Rust log `b544f5f`. |

## Approaches

| # | Approach | Worst case | Verdict |
|---|---|---|---|
| **A** | New `docs/` + CHARTER pointer + journal + thin `AGENTS.md`. Do not touch leftover README. | Docs drift if someone later edits leftover README as land. | **Chọn.** |
| B | Copy-aside leftover README, land-edit HEAD README "Xong tới đâu". | Leftover mint miss = fold. | Reject this wave. |
| C | Recook isolate / fill 03 because "kế hoạch còn". | P tasks already 100%. That is unpaid company. | Reject. |

## Planned product remainder

P `ak plan status` = 3/3 phases, 5/5 tasks. No unpaid product hunk in 0054. Next product cook is not this wave.

## Herdr

New `w13` tabs, `--no-focus`, cwd dory, 4–6 pane, OMP skills ON. Red-team tab đối soát. Không split `t13`.
