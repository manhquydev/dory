---
type: scout
date: 2026-08-21
status: DONE
---

# Scout Report: Dory (local, giấy)

## Summary

Kho `/home/manhquy/Downloads/dory` là giấy. Hai file người: `README.md`, `HIEN-PHAP.md`. Không có `package.json`, `src/`, máy, hay vòng agent. Cổng chết động cơ còn đóng cho đến khi hai báo cáo xia `--compare` được ký trong repo này.

## Relevant Files

- `HIEN-PHAP.md` — luật nhà. Bốn hộp: thẩm phán (`flow-skill`), bảng (`flow-deck`), máy phiên (Dory động cơ 1), cửa sổ chỗ làm (Dory động cơ 2). Học Harness rồi Herdr. Cấm `/ak:plan`, `--port` / `--copy` / `--fast` trước hai chữ ký. Hàng xuất cấm gọi `dsh` / `herdr` lúc chạy.
- `README.md` — nhắc cửa sổ phải là thư mục này; không học từ nhà bảng.
- `plans/reports/260821-1416-research-deepseek-harness.md` — brief researcher (cùng lượt).

## Local map (integration surface)

| Surface | Where | Status |
|---|---|---|
| Máy phiên | Dory engine 1 (paper) | Chưa viết |
| Nhật ký phiên là sự thật | Same box, paper law | Chưa viết |
| Duyệt | In Harness-learn list | Chưa viết |
| Web local | In Harness-learn list | Chưa viết |
| Cửa sổ thẻ ô | Engine 2; Herdr later | Out of this compare |
| Gọi `flow.sh` như máy chủ lạ | Arrow in constitution | EXISTS in `flow-skill`, not in this repo |
| Deck / skill tree | Forbidden homes | Must not write reports there |

Neighbor (read only, not a Dory file): `flow-skill` owns gates and receipts, never the runtime (`docs/adr/0001-discipline-layer-identity.md`). Dory later calls `flow.sh`. Flow must not contain `dory`.

## Patterns

- Paper-only until both signed compares + staffing/burn letter.
- Two engines, one family. Not a plugin marketplace. Not a deck upgrade.
- Learn, do not rent another product's loop.

## Unresolved Questions

- Operator has not signed this Harness compare yet.
- Herdr compare not started.
- No local code to map file-for-file against dsh packages.
