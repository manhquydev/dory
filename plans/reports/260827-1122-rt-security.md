# RT Security + Fact Check — 260827-1122 AOE 5 isolate flow judge

Lens: Security Adversary + Fact Checker. Plan review only. Land cites via `git show HEAD:path`. Leftover README/attach/main/server/p5_attach are dirty on purpose — not land.

Scope: factory sock spawn, leftover/isolate ELF argv on factory XDG, FLOW_BIN herdr/dsh, ancestor-walk to Downloads/flow, DORY_*/HOME/PI_CODING_AGENT_DIR leaks, stop-compound shooting factory, sit t13/wP.

Scout: attach without isolate XDG; stop one-liner without identity gate; Phase 1 start line drops XDG; FLOW_PROJECT_ROOT inherit vs walk lie; FLOW_BIN=herdr rc=1 collision; sit id equality; taxi HOME; PI_CODING factory store.

---

## Finding 1: Attach step drops isolate XDG — leftover ELF mints factory sock
- **Severity:** Critical
- **Location:** phase-01-start.md Implementation Steps §7; plan.md Bẫy 8
- **Flaw:** Sit attach is specified as `env -u DORY_SOCKET -u DORY_ENV -u DORY_RECYCLE` + `DORY_SKIP_ONBOARD=1` only. Paid 1910 attach prefixes `XDG_RUNTIME_DIR=$ISO_REAL` and `"$SIT_DORY" attach`. Land ping is DORY_SOCKET-first, then `$XDG_RUNTIME_DIR/dory/default/dory.sock`. Leftover `ensure_server` still `Command::new(exe).arg("server")` on miss. Phase 1 refuse checks SIT_DORY identity, not argv0 of the attach line.
- **Failure scenario:** Cook copies step 7 literally and types `dory attach` (leftover ELF at `rust/target/debug/dory`) into the sit pane, or send-texts attach without isolate XDG. Leftover `spawn_server` binds factory `${XDG_RUNTIME_DIR}/dory/default/dory.sock`. Founder default is now live. Isolate C ELF on the same factory XDG does not spawn — it pings factory or prints `server not running` — so sit needles never appear unless leftover ELF is the argv.
- **Evidence:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-01-start.md:56`; `dory/scripts/dory-isolate-flow-sit.sh:331`; `git show HEAD:rust/src/attach.rs:326-337`; `git show HEAD:rust/src/server.rs:1838-1846`; `git show HEAD:rust/src/socket.rs:90-95`; leftover `dory/rust/src/attach.rs:379-452` (`spawn_server`, not land)
- **Suggested fix:** Copy 1910 ATTACH_CMD verbatim: `cd "$ISO_REAL" && DORY_SKIP_ONBOARD=1 XDG_RUNTIME_DIR=$ISO_REAL env -u DORY_SOCKET -u DORY_ENV -u DORY_RECYCLE "$SIT_DORY" attach`. Refuse leftover ELF / isolate ELF / PATH `dory` as argv0 on factory XDG (`/proc/*/cmdline` vs FACTORY_XDG). `factory_must_dead` immediately after attach.

## Finding 2: Stop is a naked one-liner — identity miss shoots factory
- **Severity:** Critical
- **Location:** plan.md Constraints + Bẫy 7; phase-01-start.md step 12
- **Flaw:** Plan copies only `XDG_RUNTIME_DIR=$ISO_REAL env -u DORY_SOCKET "$SIT_DORY" server stop`. Paid `compound_stop` aborts unless ISO is a real dir, not FACTORY_XDG, not leftover isolate, sock is a non-symlink under ISO_REAL, and realpath equals ISO_SOCK. Land `stop()` is `send_op` → `connect_control`: nonempty `DORY_SOCKET` wins; empty `DORY_SOCKET=` falls through to factory XDG default sock.
- **Failure scenario:** Mint collision or `ISO_REAL=$FACTORY_XDG`. Or `ISO_REAL/dory/default/dory.sock` is a symlink to `/run/user/1000/dory/default/dory.sock`. Or cook writes `DORY_SOCKET= "$SIT_DORY" server stop` (empty assign, the exact abort law) while factory XDG is still the process env. Stop RPC hits founder sock. Desk dies. End-of-script `connectable=0` then “passes.”
- **Evidence:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/plan.md:21,64`; `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-01-start.md:61`; `dory/scripts/dory-isolate-flow-sit.sh:69-99`; `dory/scripts/dory-isolate-flock-prompt.sh:95-125`; `git show HEAD:rust/src/server.rs:94-95,1824-1829,1838-1846`
- **Suggested fix:** Mandate 1910 `iso_identity_ok` + symlink/realpath/under-ISO gates before the one-liner. Abort (do not stop) on identity fail. Never `iso()` / `DORY_SOCKET=` on stop. Probe factory sock after stop; if it flipped, FAIL hard.

## Finding 3: Phase 1 start line drops XDG — isolate ELF binds factory default
- **Severity:** Critical
- **Location:** phase-01-start.md step 4 vs Architecture; phase-02-independent-aoe-5-test.md step 1; plan.md Bẫy 8
- **Flaw:** Step 4 says “Start isolate server (0242 HOME/PATH/PI_CODING)” — three env names, not `XDG_RUNTIME_DIR=$ISO_REAL`. Land `session_paths` binds `$XDG_RUNTIME_DIR/dory/<session>/dory.sock`. Explicit `server` always binds; C `ensure_server` only refuses implicit spawn. Phase 2 “refuse leftover-or-isolate ELF argv on factory XDG” is a slogan: no `/proc` recipe, no FACTORY_XDG compare. 1032 already required a hard argv ban independent of `DORY_*`.
- **Failure scenario:** Cook copies HOME/PATH/PI from 0242 and runs `"$SIT_DORY" server` under factory XDG (`/run/user/1000`). Isolate ELF (land-4b70f79) creates `/run/user/1000/dory/default/dory.sock`. That is factory occupancy. Later `XDG_RUNTIME_DIR=$ISO_REAL … server stop` misses it. `herdr server stop` is banned, so founder sock stays up — or a later mistaken factory-XDG stop kills it (Finding 2).
- **Evidence:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-01-start.md:23,53`; `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-02-independent-aoe-5-test.md:33`; `dory/scripts/dory-isolate-flock-prompt.sh:346-352`; `git show HEAD:rust/src/socket.rs:46,90-95`; `git show HEAD:rust/src/attach.rs:326-331`; `dory/plans/260827-1032-ensure-server-no-auto-spawn/plan.md:42,54`
- **Suggested fix:** Step 4 must be the full 0242 `setsid env -u DORY_SOCKET … XDG_RUNTIME_DIR="$ISO_REAL" … server` line. After bind: isolate sock connectable AND `factory_must_dead`. Test/review: scan `/proc/*/cmdline` for leftover `…/dory/rust/target/debug/dory` and isolate `…/land-4b70f79/…/dory` whose environ `XDG_RUNTIME_DIR` equals FACTORY_XDG.

## Finding 4: Ancestor-walk-to-Downloads/flow is false; factory FLOW_* inherit is the real judge leak
- **Severity:** High
- **Location:** plan.md Bẫy 4; phase-01-start.md taxi helper + Risk; research-01 §1 item 3
- **Flaw:** `flow.sh` ancestor walk only climbs parents of `$PWD` and only if `$PWD` has neither `flow/` nor `cards/`. ISO under `~/.cache/dory-isolates/aoe5.XXXXXX` cannot adopt `~/Downloads/flow` (sibling of `.cache`, not an ancestor). An explicit `FLOW_PROJECT_ROOT` always wins. Phase 1 refuse lists factory `DORY_*`/`PI_CODING_AGENT_DIR`, not `FLOW_*`. Cited research-01 orders taxi **not** to set `FLOW_PROJECT_ROOT`. Plan helper sets it. Cook following the research source drops the only inherit override.
- **Failure scenario:** Factory pane already has `FLOW_PROJECT_ROOT=/home/manhquy/Downloads/flow/flow-skill` (that tree has `flow/00-idea.md`). Cook follows research-01, omits the helper export, cds to ISO. `flow.sh:30-31` judges the factory skill tree, not `$ISO_REAL/flow/00-idea.md`. Taxi 1 can still be rc=1 (unchecked/FILL on the factory file). Occupant Writes the isolate path. Taxi 2 still judges factory. Or occupant/coord “fixes” `flow-skill/flow/00-idea.md` — factory mutation outside leftover-5 hash. Phase 1 risk signal “stdout mentions `Downloads/flow` without `/hello`” is leftover from unpaid `hello/` layout.
- **Evidence:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/plan.md:61`; `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-01-start.md:28-34,50,73`; `dory/plans/reports/260827-1116-research-01-flow-judge.md:41`; `/home/manhquy/.claude/skills/flow/runner/flow.sh:24-46`; `flow-skill/flow/00-idea.md` (factory tree)
- **Suggested fix:** Refuse factory `FLOW_PROJECT_ROOT`/`FLOW_BIN`/`FLOW_GLOBAL_KB` at script entry (same class as `DORY_*`). Pin `FLOW_PROJECT_ROOT=$ISO_REAL` on the taxi `env` line (override inherit). Delete the Downloads/flow walk claim. Assert journal `cwd` + gate stdout paths are under `ISO_REAL`, not `Downloads/flow`.

## Finding 5: FLOW_BIN=herdr|dsh is executable — taxi 1 rc=1 is a false gate
- **Severity:** High
- **Location:** phase-01-start.md step 1 + step 6; plan.md Scout + Bẫy 5; research-01 traps
- **Flaw:** Land refuses basename `herdr`/`dsh` and returns 1 **before** journal (`resolve_flow_bin` / `forbidden_name`). Phase 1 refuse is “`FLOW_BIN` not executable.” `/usr/bin/herdr` is executable. Taxi 1 acceptance is `rc=1`. `FLOW_BIN="${FLOW_BIN:-…/flow.sh}"` keeps a factory herdr pin. Empty `FLOW_BIN` becomes `"flow.sh"` PATH miss — also rc=1, no `GATE stage 00-idea`.
- **Failure scenario:** Cook tab inherits `FLOW_BIN=herdr` (or a path whose basename is `dsh`). Refuse passes. Taxi 1 exits 1 with `dory: refusing to exec herdr`. Sloppy “last-but-needed” journal read sees no new `flow/result` or reads a prior event. AOE 5 “fail then pass” is a taxi refuse, not `scan_gate`. Phase 2 `bin == abs flow.sh` only helps if two `flow/result` rows exist.
- **Evidence:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-01-start.md:50,55`; `dory/plans/260827-1122-aoe-5-isolate-flow-judge/plan.md:49,62`; `git show HEAD:rust/src/flow.rs:39-51,121-146`; `dory/plans/reports/260827-1116-research-01-flow-judge.md:223`; `dory/scripts/dory-isolate-flow-sit.sh:308` (`FLOW_BIN=/bin/true` chrome — opposite rc)
- **Suggested fix:** Refuse factory `FLOW_BIN` if set. Pin argv0 to the abs skill `flow.sh` after `realpath` + byte/identity check (not p5 `flow-skill` copy). Taxi 1 must require a new `flow/result` with `bin`=that path, `code=1`, stdout `GATE stage 00-idea`. Treat missing journal as FAIL, not as gate-fail.

## Finding 6: Sit refuse `= t13/p2R/wP` does not match factory ids — send-text hits t13/wP
- **Severity:** High
- **Location:** phase-01-start.md step 1; plan.md Herdr + Bẫy 6; phase-03-review-and-ship.md step 4
- **Flaw:** Phase 1 writes `sit pane = t13/p2R/wP`. Paid law is exact `w13:p2R`, glob `*wP:*`, exact `w13:t13` on both `SIT_TAB` and `herdr pane get` `tab_id`, plus sit pane ≠ cook pane and no agent. Herdr ids are `w13:t13`, not `t13`.
- **Failure scenario:** Cook implements `[ "$SIT_PANE" = "t13" ]` / `[ "$SIT_TAB" = "t13" ]`. Factory `SIT_PANE=w13:t13` passes. `send-text` + `enter` types isolate `attach` into founder sit. Leftover ELF attach (Finding 1) then spawns or recycles factory. Phase 3 “close wave tabs only; leave t13” cannot undo keystrokes already delivered to t13. Split-t13 is banned in prose, not in the refuse list.
- **Evidence:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-01-start.md:50`; `dory/plans/260827-1122-aoe-5-isolate-flow-judge/plan.md:63,79`; `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-03-review-and-ship.md:69`; `dory/scripts/dory-isolate-flow-sit.sh:189-217`; `dory/scripts/dory-isolate-flock-prompt.sh:230-240,258-259`
- **Suggested fix:** Copy 1910/0242 pane/tab refuses byte-for-byte, including post-mint `GOT_TAB` re-check. `rv_sit` accepts only if sit pane/tab ≠ `w13:t13`/`w13:p2R`/`*wP:*` and `herdr pane get` agrees.

## Finding 7: Taxi keeps factory HOME; plan HOME path is `$ISO/home` not `$ISO_REAL/home`
- **Severity:** High
- **Location:** plan.md Bẫy 16; phase-01-start.md Architecture + taxi helper + step 1
- **Flaw:** Trap 16 bans `export HOME` on factory and sets isolate **server** `HOME=$ISO/home`. Paid 0242 uses `HOME="$ISO_REAL/home"`. `ISO` is the mktemp path; `ISO_REAL` is `realpath`. Taxi helper does not set HOME. `flow.sh` `GLOBAL_LOG` is `${HOME}/.claude/flow/usage.jsonl` and appends unless `FLOW_LOG_DISABLE`/`DO_NOT_TRACK`. Research-02 taxi sketch omits those two flags. Phase 1 step 1 does not copy 1910 `HOME != FACTORY_HOME` refuse.
- **Failure scenario:** Cook follows trap 16 / Architecture and exports `HOME=$ISO/home` for the whole script (or only on server, leaving taxi on factory HOME). Taxi `flow.sh` appends usage to factory `~/.claude/flow/usage.jsonl` and reads `${HOME}/.claude/flow/playbooks`. If `ISO` ≠ `ISO_REAL`, server HOME is a missing or aliased dir; occupants write grok/omp state off the wipe tree. Script-level `export HOME` also retargets factory `git`/`herdr` for the rest of cook.
- **Evidence:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/plan.md:73`; `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-01-start.md:23,28-34,50`; `dory/scripts/dory-isolate-flow-sit.sh:37-40`; `dory/scripts/dory-isolate-flock-prompt.sh:348`; `/home/manhquy/.claude/skills/flow/runner/flow.sh:23,90,3760-3761,3852-3856`; `dory/plans/reports/260827-1116-research-02-isolate-project.md:100-102`
- **Suggested fix:** Refuse `HOME != FACTORY_HOME` at entry (1910). Never `export HOME` at script scope. Server + taxi: `HOME="$ISO_REAL/home"` only on the prefixed command. Keep `FLOW_LOG_DISABLE=1` `DO_NOT_TRACK=1` on taxi. Assert factory `~/.claude/flow/usage.jsonl` mtime/size unchanged.

## Finding 8: PI_CODING_AGENT_DIR points occupants at factory `~/.omp/agent`; coord text is weaker than 0242
- **Severity:** High
- **Location:** plan.md Bẫy 15; phase-01-start.md Architecture + step 8
- **Flaw:** Isolate server is ordered to set `PI_CODING_AGENT_DIR=$FACTORY_HOME/.omp/agent` (0242 occupant need). Occupant PTYs inherit it. 0242 coord inner text forbids read/write of `~/.omp`, `agent.db`, and `PI_CODING_AGENT_DIR`. Phase 1 step 8 only forbids `herdr`, `server stop`, `--wait`, “factory paths.” Taxi/attach helpers do not `env -u PI_CODING_AGENT_DIR`.
- **Failure scenario:** omptest is told to Write `$ISO_REAL/flow/00-idea.md` via omp tools. Same Write can hit factory `~/.omp/agent` (agent.db, skills, session). That is a factory store leak/mutation. Leftover-5 hash and repo `.dory/` snap do not cover `~/.omp`. Trap 15 only refuses the var **set on factory** at script entry — after server start the var is live inside isolate panes by design.
- **Evidence:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/plan.md:72`; `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-01-start.md:23,57`; `dory/scripts/dory-isolate-flock-prompt.sh:350-351,501`; `dory/scripts/dory-isolate-flow-sit.sh:277-278` (1910 unsets PI on server — no occupants)
- **Suggested fix:** Keep PI set **only** on the server `setsid` line. Coord/omptest prompt must include 0242’s `~/.omp` / `agent.db` / `PI_CODING_AGENT_DIR` ban plus exact PASS bytes and the isolate path. Snap factory `~/.omp/agent` mtime/ino; FAIL on change.

## Finding 9: Leftover-5 snap is unnamed; paid snap is six paths including desk.rs
- **Severity:** Medium
- **Location:** phase-01-start.md step 2; plan.md Success Criteria; 0242 `leftover_hash_snap`
- **Flaw:** Plan says “Snap leftover 5 `git hash-object`” and never lists paths. Paid 0242 hashes README + attach + main + server + p5_attach **and** `rust/src/desk.rs` (six). User leftover mint is the five dirty files; `desk.rs` is the glance land file this slice must not touch.
- **Failure scenario:** Cook snaps only `scripts/` or omits `attach.rs`/`p5_attach.rs`. Leftover ELF spawn hunk or glance edit slips. Or cook copies 0242 and then treats a `desk.rs` hash drift as leftover-5 (false FAIL) / ignores a dirty attach because they thought leftover 5 = README plus four rust sources without tests.
- **Evidence:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-01-start.md:51`; `dory/plans/260827-1122-aoe-5-isolate-flow-judge/plan.md:106`; `dory/scripts/dory-isolate-flock-prompt.sh:162-166`
- **Suggested fix:** Name the five dirty mint paths plus a separate `desk.rs` must-match-HEAD hash. Copy 0242 `leftover_hash_snap` and assert `git show HEAD:rust/src/desk.rs` still equals worktree `desk.rs`.

---

## Plan status
Phases 1–3 still Pending. Do not cook until Findings 1–3 (sock spawn / stop / ELF-on-factory-XDG) are written into the script contract with 1910/0242 identity gates, and Findings 4–8 are in the refuse/taxi/attach/prompt lines. Finding 9 before leftover snap is coded.
