# REVIEW pane — spawn hunk only

You are `rv_spawn`. Skills ON (ak:code-review). Read-only.

Prove or reject: `git show HEAD:rust/src/attach.rs` `ensure_server` has no `arg("server")` spawn; eprintln locked; `sit` + `desk::run_with_pane` still call it; `dory server` still `run_foreground`.

Cite HEAD blobs only. Never leftover `attach.rs:379`.

Do not invoke dory. Do not cargo leftover. Do not sit t13.

Write findings to `/home/manhquy/Downloads/flow/dory/plans/reports/260827-ensure-server-no-spawn-review-spawn.txt`
Critical 0 required for ACCEPT.
End: `SPAWN_ACCEPT` or `SPAWN_REJECT`.
