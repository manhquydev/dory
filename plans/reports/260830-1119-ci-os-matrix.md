---
type: ops
date: 2026-08-30
time: 11:19
head: b6b93ee
---

# Operator — what is left

## Already done (do not redo)

- Repo **public**: https://github.com/manhquydev/dory
- Env `npm-publish` + reviewer `manhquydev`
- Workflow publish: tag `npm@*` / dispatch `dry_run` (default true)
- Package `@manhquy/dory` bin **`dory-serve`** (not desk `dory`)
- Leftover 5 still local `M`

## You click (npmjs, account `@manhquy`)

Trusted Publisher for **new** package `@manhquy/dory`:

1. owner `manhquydev`
2. repo `dory`
3. workflow `publish-npm-wrapper.yml`
4. environment `npm-publish`
5. action **publish**

Then Actions → Publish → version `0.1.0-next.0` → **keep `dry_run=true`**. Only then `false` or tag `npm@0.1.0-next.0`.

Do not laptop `npm publish --provenance`. Do not paste npm tokens into chat. Dist-tag add is still manual (OIDC E401) — same as flow-skill.

## What CI now proves

| Gate | OS | Meaning |
|---|---|---|
| `rust-desk` | Linux, macOS | `cargo test --locked` desk (no factory `FLOW_BIN` siblings: `p5_inside` / `p5_s11` / `p5_real_repo` / `p5_live_loop`) |
| `rust-windows-notice` | Windows | Honest skip — crate is unix sockets |
| `npm-wrapper` | Linux, macOS, Windows × Node 22/24 | lamp `dory-serve` only |

Windows desk is unpaid. Do not claim `npm i -g` is `dory` desk.
