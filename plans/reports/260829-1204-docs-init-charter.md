---
type: docs-init
lens: charter
date: 2026-08-29
time: 12:04
result: ACCEPT
head: 049e304
p_feat: f1c966c
rust_land: b544f5f
---

# CHARTER pointer

Working `CHARTER.md` is not leftover 5. Diff vs `HEAD` is one pointer, inserted after the cold-read line, before `## Bốn hộp`.

WHERE link count: **1**. Target: `[docs/README.md](docs/README.md)`. String `WHERE` occurs once. Link inventory otherwise: north-star, `CAPACITY-FREEZE.md` (twice). No second docs index.

Kill conditions **unchanged**. `## Điều kiện giết` through the five bullets is byte-identical to `git show HEAD:CHARTER.md`. Bullets still:

- Nhét Dory vào repo hoặc lệnh `flow-deck`.
- Nhét Dory vào `@manhquy/flow-skill` hoặc cây skill của flow.
- Hàng xuất Dory gọi `dsh` / `herdr` như vòng chạy thật.
- Có commit động cơ trước khi hai báo cáo xia được ký. (lịch sử; không mở lại)
- Xia chạy từ cửa sổ `flow-deck` rồi ghi báo cáo vào `flow-deck/plans/`.

No Phase 5 claim. No leftover fold.

Land README is `git show HEAD:README.md` (blob `5ac82b10`). Working leftover README stays mint `68190a5f` and `M`. Not folded into CHARTER.

`docs/README.md` exists as the WHERE owner. CHARTER remains WHY (two chairs, kill, hình B).
