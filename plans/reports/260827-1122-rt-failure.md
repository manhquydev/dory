---
type: review
lens: rt-failure
date: 2026-08-27
time: 11:24
plan: 260827-1122-aoe-5-isolate-flow-judge
head: ee0350cf
rust_land: b544f5f
authority: git show HEAD + plan/phase grep. No dory. No default. No leftover ELF.
---

# RT failure — AOE 5 isolate flow judge (plan)

Hostile plan review. Scope only: occupant-write vs factory-write, taxi1/taxi2 journal order, sit attach send-text vs pane run, glance world.cwd vs journal path, FLOW_PROJECT_ROOT, wait-output needles `Flow 1. gate` / `Flow 0. gate`, isolate wipe, omp `prompt --wait` stall, script exec of paid scripts.

HEAD blobs via `git show HEAD`. Isolate ELF land = rust `b544f5f`. Do not cite leftover worktree `server.rs` as land (working `workspace_live_cwd` ≠ HEAD `desk_tree`).

## Finding 1: No-wait prompt then immediate taxi 2 races the occupant Write
- **Severity:** Critical
- **Location:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-01-start.md:57-59`
- **Flaw:** Step 8 forbids `--wait` (correct vs 0242 trap 10) then step 9 taxis immediately. HEAD `agent prompt` without `--wait` returns as soon as the occupant's word leaves `idle`, not when omptest finished writing `00-idea.md`. Paid 0242 does **not** taxi after prompt — it polls world-state for 300s. This plan has no poll for PASS bytes / omptest idle.
- **Failure scenario:** Factory mints FAIL template. `coord prompt --timeout` returns in milliseconds after stall-check. Taxi 2 still sees `- [ ]` / `[FILL]`. `flow.sh gate 00-idea` exits 1. Script FAIL. Cook "fixes" it by factory-writing PASS (Finding 2) or by adding `--wait` (Finding 7). AOE 5 hole (occupant artifact) is never paid.
- **Evidence:** `phase-01-start.md:57-59` (prompt then taxi 2, no poll). `phase-01-start.md:72` treats miss as occupant fault, not a sequencing hole. `scripts/dory-isolate-flock-prompt.sh:500-504` prompt has no `--wait`. `scripts/dory-isolate-flock-prompt.sh:516-534` 300s poll for decoded workspace list. `git show HEAD:rust/src/server.rs:1569-1578` — `wait==false` returns snapshot immediately after stall loop.
- **Suggested fix:** After prompt, poll until `$ISO_REAL/flow/00-idea.md` is `scan_gate` clean **or** timeout. Then taxi 2. Forbid factory `cat`/`tee`/`python` write of that path. Copy 0242's poll shape, change the needle to file bytes.

## Finding 2: Occupant PASS authorship is prose; factory already writes the idea file
- **Severity:** High
- **Location:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/plan.md:59` · `phase-01-start.md:37-39` · `phase-03-review-and-ship.md:29`
- **Flaw:** Factory **must** write FAIL `00-idea.md` (mint). PASS bytes are embedded in the factory script as the coord prompt. Journal / sit / leftover asserts cannot see uid or tool. `rv_judge` says "occupant wrote PASS" with no procedure. Re-prompt-then-STOP is honor code. Cook under Finding 1 pressure will `printf` the embedded PASS heredoc onto disk and taxi 2 will go green.
- **Failure scenario:** Receipt shows codes `1` then `0`, `bin` = abs `flow.sh`, sit `Flow 0. gate`. Review ACCEPT. Occupant never Wrote. AOE 5 is 1910 chrome plus factory `sed`.
- **Evidence:** `plan.md:59` "Factory viết file PASS rồi taxi 2 — **cấm**" (ban, not a check). `phase-01-start.md:37-39` "Exact content the coord prompt tells `omptest` to Write" — bytes live in factory script. `phase-01-start.md:51` factory mint FAIL. `phase-03-review-and-ship.md:29` accept iff occupant wrote; no mtime/transcript/grep. `git show HEAD:rust/src/flow.rs:87-100` journals bin/argv/code/stdout only — no author.
- **Suggested fix:** After taxi 1, hash FAIL file. After prompt poll, require hash **changed** and matches the prompt bytes. `rg` the new script: no write to `00-idea.md` except the FAIL mint. Review reads occupant `recent-unwrapped` for a Write of that abs path. Factory PASS write = FAIL the receipt.

## Finding 3: Attach command drops isolate XDG; C no-spawn dies on factory sock
- **Severity:** Critical
- **Location:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-01-start.md:56` · `plan.md:68`
- **Flaw:** Phase 1 attach is only `env -u DORY_SOCKET -u DORY_ENV -u DORY_RECYCLE` + `DORY_SKIP_ONBOARD=1`. Paid 1910/0242 attach is `cd "$ISO_REAL" && DORY_SKIP_ONBOARD=1 XDG_RUNTIME_DIR=$ISO_REAL env -u … "$SIT_DORY" attach`. HEAD `ensure_server` ping-miss → `Err(1)`, **no spawn**. Unset `DORY_SOCKET` without isolate `XDG_RUNTIME_DIR` pings factory `/run/user/1000/dory/default/dory.sock` (dead). Sit never paints. `wait-output` `Flow 1. gate` times out. Plan correctly bans `herdr pane run` for the TUI, then underspecifies the send-text payload so attach never connects.
- **Failure scenario:** Cook copies phase-01 literally. `send-text` + `enter` runs isolate ELF against factory XDG. Attach prints `dory: server not running; start with \`dory server\`` and exits. Footer never `Flow 1. gate`. Journal may still be correct. Phase 2 says sit miss = TEST_FAIL, do not recook `desk.rs` — cook then factory-skips sit or uses `pane run` (TUI hang + factory XDG).
- **Evidence:** `phase-01-start.md:56` attach env list (no `XDG_RUNTIME_DIR=$ISO_REAL`, no `cd "$ISO_REAL"`). `plan.md:68` send-text vs pane run only. `scripts/dory-isolate-flow-sit.sh:331-337` full `ATTACH_CMD`. `scripts/dory-isolate-flock-prompt.sh:457-462` same. `git show HEAD:rust/src/attach.rs:326-331` no-spawn. `git show HEAD:rust/src/desk.rs:446-453` glance only after attach is in `loop_ui`.
- **Suggested fix:** Paste 1910 `ATTACH_CMD` verbatim. Refuse `herdr pane run` **and** `dory pane run` for attach. Preflight: factory sock still FileNotFound after send-text.

## Finding 4: Copy-law wipe deletes the journal phase 2 independently measures
- **Severity:** High
- **Location:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-02-independent-aoe-5-test.md:23-37` · `phase-01-start.md:61`
- **Flaw:** Phase 2: script "tự mint + wipe"; then python-read `$PROJECT/.dory/sessions/s1.jsonl` / `$ISO_REAL/.dory/sessions/s1.jsonl` after the script. Copy-law wipe is 1910 `teardown` → `rm -rf -- "$ISO_REAL"` on EXIT. 1910 only keeps journal because it `JOURNAL_SNAP="$(cat "$JOURNAL")"` **before** teardown. Phase 1 step 12: stop + cook receipt; no snap. Phase 2 risk names "second mint collide" — `mktemp aoe5.XXXXXX` does not collide.
- **Failure scenario:** Cook copies 1910 wipe. Script exit 0. Test opens the printed journal path. ENOENT. TEST_FAIL on a green cook. Or cook skips wipe "so test can read"; orphan isolate server + sock under `~/.cache/dory-isolates/aoe5.*`. Phase 2 only fail-closes factory sock, not leftover isolate socks.
- **Evidence:** `phase-02-independent-aoe-5-test.md:23` mint+wipe + read `$PROJECT/.dory/sessions/s1.jsonl`. `phase-02-independent-aoe-5-test.md:37` path `$ISO_REAL/.dory/sessions/s1.jsonl`. `phase-02-independent-aoe-5-test.md:55` collide/factory-XDG (wrong signal). `phase-01-start.md:61` leftover then stop; no `JOURNAL_SNAP`. `scripts/dory-isolate-flow-sit.sh:103-118` wipe. `scripts/dory-isolate-flow-sit.sh:150-158` EXIT teardown wipe. `scripts/dory-isolate-flow-sit.sh:355-356` snap before teardown. `git show HEAD:rust/src/flow.rs:115-118` journal lives under workspace cwd, i.e. inside ISO.
- **Suggested fix:** Snapshot journal bytes + ISO path to the receipt **before** `compound_stop`/`wipe`. Phase 2 asserts on the snap file, not a path the script just deleted. If wipe is required, also `pgrep`/`ss` leftover `aoe5.*` socks. Drop "mint collide".

## Finding 5: Taxi1/taxi2 journal order is not an ordered pair of two results
- **Severity:** High
- **Location:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/phase-01-start.md:55` · `phase-02-independent-aoe-5-test.md:38-40`
- **Flaw:** "Journal last-but-needed" is undefined after one taxi and wrong after two (last is taxi 2). Phase 2: `≥2` results, **first** `code==1`, **last** `code==0`. Extra factory taxis in the middle still pass. Glance reads **last** `flow/result` only — after taxi 2 the footer cannot show `Flow 1. gate`.
- **Failure scenario:** Taxi 1 (1). Occupant miss. Taxi 2 (1). Factory writes PASS. Taxi 3 (0). First=1 last=0. Receipt green. Or cook taxis `status` then `gate` and "first judged" is ambiguous vs `flow/invoke` lines. Sit after taxi 2 only ever paints `Flow 0. gate`.
- **Evidence:** `phase-01-start.md:55` "last-but-needed". `phase-02-independent-aoe-5-test.md:38-40` first/last, not exactly-two-in-order. `git show HEAD:rust/src/flow.rs:67-102` each taxi appends `flow/invoke` then `flow/result`. `git show HEAD:rust/src/desk.rs:2363-2376` `last_flow_result_bytes` rsplit, first `flow/result` wins (last in file). `git show HEAD:rust/src/desk.rs:5009-5018` two results → glance is the final code only.
- **Suggested fix:** Assert **exactly two** `flow/result` events, order `code=1` then `code=0`, same `bin`, argv0 `gate`. Name them taxi1/taxi2 in the receipt. Do not say "last-but-needed".

## Finding 6: Sit needle `Flow 0. gate` is `/bin/true` chrome
- **Severity:** High
- **Location:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/plan.md:20` · `phase-01-start.md:56,59`
- **Flaw:** Glance paints `Flow {n}. {arg0}` from last result. It does **not** paint stdout or `bin`. `FLOW_BIN=/bin/true -- gate 00-idea` → footer `Flow 0. gate` (code 0, arg0 `gate`). That is 1910 chrome with a different token. `Flow 1. gate` is the only sit needle true cannot fake. `wait-output --source visible` after taxi 2 **replaces** `Flow 1. gate` in the footer; phase 2 cannot independently re-see taxi 1 on a live pane once glance advanced (and wipe/stop kills attach).
- **Failure scenario:** Cook pins `FLOW_BIN=/bin/true` "to debug sit". Taxi 1 still exits 0 (true). If they skip the rc=1 assert, sit still shows `Flow 0. gate`. Or they use real `flow.sh` for taxi 1 and true for taxi 2: sit needles both match, journal `bin` check is the only backstop — phase 2 allows any two results as long as first/last codes match if they also forget `bin`. Overlay (`git show HEAD:rust/src/desk.rs:5148-5156`) steals footer; 20s wait-output miss on a correct journal.
- **Evidence:** `plan.md:20` sit footer `Flow 1. gate` rồi `Flow 0. gate`. `plan.md:50` glance does not paint stdout; `true -- status` and `flow.sh -- status` both `Flow 0. status`. `git show HEAD:rust/src/desk.rs:3450-3458` format. `git show HEAD:rust/src/desk.rs:2325-2359` last `flow/result` + `args[0]` only. `scripts/dory-isolate-flow-sit.sh:308-309` 1910 `FLOW_BIN=/bin/true`. `plans/reports/260827-1116-research-01-flow-judge.md:204` `flow.sh -- gate` footer `Flow 1. gate`.
- **Suggested fix:** Sit needles are chrome, not the judge. Gate sit on unique taxi1 token **and** journal `bin`. Do not treat `Flow 0. gate` as PASS. Dump visible after each wait. Phase 2 sit = receipt of both waits, not a post-wipe pane read.

## Finding 7: Using `--wait` to close Finding 1 reopens the 0242 unknown stall
- **Severity:** High
- **Location:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/plan.md:67` · `phase-01-start.md:57`
- **Flaw:** Non-goal forbids `--wait` / `occ.report=Working` but the only specified occupant sync is the prompt itself. Cook will add `--wait` to "wait for Write". HEAD `wait_hit` is `idle|done|blocked` only. After prompt, omp comm is not allowlisted → word `unknown` until a new `agent report`. `--wait` does not hit `unknown`. Timeout 180s. Inner text already forbids `--wait`; factory CLI `--wait` is the trap 0242 paid to avoid.
- **Failure scenario:** Finding 1 fails. Cook changes step 8 to `iso_mut agent prompt coord --wait --timeout 180000`. Omptest Writes but forgets `report idle` (prompt is long). Wait loops to timeout. Script looks hung. Or they report `working` (founder word) — `wait_hit` still misses. Recook rust / `occ.report=Working` to "make wait work" — unpaid and banned.
- **Evidence:** `plan.md:22,67,113` `--wait` non-goal / trap 10. `phase-01-start.md:57` `--timeout` OK, no `--wait`. `plans/reports/260827-1012-eval-aoe.md:50` after clear, omp is `unknown`; `wait_hit` idle\|done\|blocked. `git show HEAD:rust/src/server.rs:1582-1588` `wait_hit`. `git show HEAD:rust/src/server.rs:1569-1575` `--wait` → `agent_wait_loop`. `scripts/dory-isolate-flock-prompt.sh:500` `--timeout 180000` only.
- **Suggested fix:** Keep `--wait` off the factory CLI **and** out of inner text. Sync on file hash (Finding 1), not occupant word. Do not reopen 0927 B.

## Finding 8: Copy-law without exec-refuse will source 1910 and taxi `/bin/true`
- **Severity:** High
- **Location:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/plan.md:60` · `phase-01-start.md:45-46,65`
- **Flaw:** "Copy law, file mới" / "does not exec paid scripts" is a success checkbox, not a script refuse. Paid 0242/1910 refuse `$0` name and say "do not source hop" **inside the paid file**. The new script has no specified `case $0`, no `rg` gate, no ban on `source`/`exec` of `dory-isolate-flow-sit.sh` / `dory-isolate-flock-prompt.sh`. Those files export `wipe_iso` **and** `FLOW_BIN=/bin/true` taxi. `iso()` on `server stop` is the abort law the plan already names — easy to import by sourcing.
- **Failure scenario:** Cook `source scripts/dory-isolate-flow-sit.sh` for wipe/attach. 1910 taxi path stays `/bin/true`. Or `bash scripts/dory-isolate-flock-prompt.sh` "to start đàn" then continue — 0242 prompt is `workspace list`, not idea Write. Sit needle `Flow 0. gate` still matches true (Finding 6). Review `rv_judge` may catch bin if they read journal; phase 1 success criteria only greps the **new** path for `exec`/`source` strings after the damage is already in a copied body.
- **Evidence:** `plan.md:60` cấm exec/source 1910/0043/0227/0242/hop. `phase-01-start.md:45-46` copy-law only. `phase-01-start.md:65` checkbox "does not exec/source paid scripts". `scripts/dory-isolate-flow-sit.sh:4,308-309` do-not-source hop; `FLOW_BIN=/bin/true`. `scripts/dory-isolate-flock-prompt.sh:5,35-38,68-76,124` do-not-source paid; `iso()` sets `DORY_SOCKET`. `plan.md:64` stop must not use `iso()`.
- **Suggested fix:** New file only. Refuse `$0` + `source`/`exec` of the four paid paths at runtime. Phase 3 `rg` those names as callers. Copy wipe/attach as pasted lines, not sourced functions.

## Finding 9: FLOW_PROJECT_ROOT threat names the wrong ancestor; leftover `/hello` signal
- **Severity:** Medium
- **Location:** `dory/plans/260827-1122-aoe-5-isolate-flow-judge/plan.md:61` · `phase-01-start.md:73`
- **Flaw:** `PROJECT=$ISO_REAL` + taxi `DORY_WORKSPACE_DIR=$ISO_REAL` **is** the right glance/journal join on HEAD. Glance path helper is `flow_journal_path(cwd)`; live cwd is desk `workspace` row, and HEAD `desk_tree` paints **`world.cwd`** (server start cwd), not leftover `workspace_live_cwd`. ISO mint is `~/.cache/dory-isolates/aoe5.XXXXXX`. `flow.sh` ancestor walk only climbs **parents of PWD**. `~/Downloads/flow` is not a parent of `~/.cache/…`. Walk adopt signature is `flow/00-idea.md` / `00-inspect.md` / `.flow/` / `cards/C-*.md` — real miss is `$HOME/flow/00-idea.md` if taxi cwd has no `flow/`. Phase 1 abort signal "stdout mentions `Downloads/flow` without `/hello`" is leftover research-02 `$ISO_REAL/hello` text. If they mint `$ISO_REAL/flow/00-idea.md` and `cd "$ISO_REAL"`, `flow/` exists and the walk **does not run**. `FLOW_PROJECT_ROOT` is belt-and-suspenders, not the Downloads/flow kill switch. Taxi rust does not inject `FLOW_PROJECT_ROOT`; shell inherit works because `Command` does not `env_clear`.
- **Failure scenario:** Cook hunts `Downloads/flow` in stdout, never sees it, ships. Taxi cwd is factory leftover or `$HOME`. Walk adopts `$HOME` or a cache ancestor with a stale `flow/00-idea.md`. Gate judges the wrong tree; rc 0/1 is not this ISO. Or cook reads leftover worktree `desk_tree` (live pane cwd), retargets `DORY_WORKSPACE_DIR` to `$ISO/home` after occupant `cd`, glance stays on `world.cwd`=`$ISO_REAL`, footer never `Flow 1. gate`.
- **Evidence:** `plan.md:61` glance `{world.cwd}/.dory/sessions/s1.jsonl` + `FLOW_PROJECT_ROOT` vs `~/Downloads/flow`. `phase-01-start.md:73` signal `Downloads/flow` without `/hello`. `git show HEAD:rust/src/flow.rs:105-118` journal = `DORY_WORKSPACE_DIR` else cwd. `git show HEAD:rust/src/desk.rs:446-447,2723-2733` glance = `workspace_cwd` → `…/s1.jsonl`. `git show HEAD:rust/src/server.rs:927-930` workspace row `cwd` = `world.cwd`. `git show HEAD:rust/src/flow.rs:162-167` child inherits env, cwd = taxi workspace. `~/.claude/skills/flow/runner/flow.sh:24-46` ancestor walk. `plans/reports/260827-1116-research-02-isolate-project.md:83,148` old `$ISO_REAL/hello` + `$PROJECT` journal (the hole this plan claims to close).
- **Suggested fix:** Keep `PROJECT=$ISO_REAL` + `DORY_WORKSPACE_DIR=$ISO_REAL` + mint `$ISO_REAL/flow/00-idea.md`. Keep `FLOW_PROJECT_ROOT=$ISO_REAL` as pin, not as Downloads/flow mythology. Abort if journal `cwd` / stdout `ROOT` ≠ `$ISO_REAL`. Cite `git show HEAD:rust/src/server.rs:927-930`, not leftover live-cwd. Drop `/hello` from the abort signal.

## Scope coverage

| Topic | Findings |
|---|---|
| occupant-write vs factory-write | 1, 2 |
| taxi1/taxi2 journal order | 5 |
| sit attach send-text vs pane run | 3 |
| glance world.cwd vs journal path | 4, 6, 9 |
| FLOW_PROJECT_ROOT | 9 |
| wait-output `Flow 1. gate` / `Flow 0. gate` | 3, 6 |
| isolate wipe | 4 |
| omp `prompt --wait` stall | 1, 7 |
| script exec of paid scripts | 8 |

## Do not

- Recook rust / leftover 5 / 1910/0242 bodies to "fix" these.
- Start default sock. Exec leftover or isolate ELF to confirm attach.
- Treat sit `Flow 0. gate` as the judge.
- Close Finding 1 with `--wait` or factory Write.
