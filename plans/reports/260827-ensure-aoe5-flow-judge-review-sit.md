# REVIEW — rv_sit

Accept iff sit ≠ `w13:t13`/`w13:p2R`/`*wP:*`; attach = 1910 `:331` isolate XDG; stop = `compound_stop`; factory sock connectable=0; no factory `dory` argv; no `pane run` attach.

Read script + cook/test. Probe sock AF_UNIX `…/dory/default/dory.sock`.
Reply `SIT_ACCEPT` or `SIT_REJECT`.
